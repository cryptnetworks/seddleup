name: Docker Image

on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "docker-compose.yml"
      - "docker-entrypoint.sh"
      - ".env.docker.example"
      - "cloudflare/**"
      - "nginx/**"
      - "package.json"
      - "package-lock.json"
      - "next.config.mjs"
      - "postcss.config.mjs"
      - "prisma.config.ts"
      - "tsconfig.json"
      - "app/**"
      - "components/**"
      - "lib/**"
      - "prisma/**"
      - "proxy.ts"
      - "public/**"
      - "scripts/**"
      - "types/**"
      - ".github/workflows/docker-image.yml"
  pull_request:
    paths:
      - "Dockerfile"
      - ".dockerignore"
      - "docker-compose.yml"
      - "docker-entrypoint.sh"
      - ".env.docker.example"
      - "cloudflare/**"
      - "nginx/**"
      - "package.json"
      - "package-lock.json"
      - "next.config.mjs"
      - "postcss.config.mjs"
      - "prisma.config.ts"
      - "tsconfig.json"
      - "app/**"
      - "components/**"
      - "lib/**"
      - "prisma/**"
      - "proxy.ts"
      - "public/**"
      - "scripts/**"
      - "types/**"
      - ".github/workflows/docker-image.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/seddleup

jobs:
  docker:
    name: docker (${{ matrix.platform }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            artifact: linux-amd64
            manifest: true
            runner: ubuntu-latest
            qemu: false
          - platform: linux/arm64/v8
            artifact: linux-arm64-v8
            manifest: true
            runner: ubuntu-24.04-arm
            qemu: false

    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up QEMU
        if: matrix.qemu
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_NAME }}

      - name: Build image
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: ${{ matrix.platform }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.artifact }}
          cache-to: type=gha,mode=max,scope=${{ matrix.artifact }}
          provenance: false

      - name: Build and push image
        if: github.event_name != 'pull_request'
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          platforms: ${{ matrix.platform }}
          outputs: type=image,name=${{ env.IMAGE_NAME }},push-by-digest=true,name-canonical=true,push=true
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.artifact }}
          cache-to: type=gha,mode=max,scope=${{ matrix.artifact }}
          provenance: false

      - name: Export image digest
        if: github.event_name != 'pull_request'
        run: |
          mkdir -p /tmp/digests
          echo "${{ steps.build.outputs.digest }}" > "/tmp/digests/${{ matrix.artifact }}"

      - name: Upload image digest
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v7
        with:
          name: digest-${{ matrix.artifact }}
          path: /tmp/digests/${{ matrix.artifact }}
          if-no-files-found: error
          retention-days: 1

      - name: Upload manifest digest
        if: github.event_name != 'pull_request' && matrix.manifest
        uses: actions/upload-artifact@v7
        with:
          name: digest-manifest-${{ matrix.artifact }}
          path: /tmp/digests/${{ matrix.artifact }}
          if-no-files-found: error
          retention-days: 1

  runtime-probes:
    name: Docker runtime and migration probes
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Build probe image
        run: docker build -t seddleup:ci .

      - name: Run runtime probes
        run: npm run test:docker

      - name: Run optional profile probes
        run: npm run test:docker:profiles

  publish-security:
    if: github.event_name != 'pull_request'
    name: Publish security validation
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Audit locked dependencies
        run: npm run security:audit

      - name: Build image for scanning
        run: docker build -t seddleup:publish-security .

      - name: Scan publish image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: seddleup:publish-security
          severity: HIGH,CRITICAL
          exit-code: "1"
          ignore-unfixed: true

  docker-manifest:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    needs:
      - docker
      - runtime-probes
      - publish-security
    permissions:
      contents: read
      packages: write

    steps:
      - name: Download image digests
        uses: actions/download-artifact@v8
        with:
          path: /tmp/digests
          pattern: digest-manifest-*
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=ref,event=tag
            type=sha,prefix=sha-

      - name: Create multi-platform manifest
        env:
          METADATA_JSON: ${{ steps.meta.outputs.json }}
        run: |
          set -euo pipefail

          refs=()
          for digest_file in /tmp/digests/*; do
            digest="$(cat "$digest_file")"
            refs+=("${IMAGE_NAME}@${digest}")
          done

          tags=()
          while IFS= read -r tag; do
            tags+=("-t" "$tag")
          done < <(jq -r '.tags[]' <<< "$METADATA_JSON")

          docker buildx imagetools create "${tags[@]}" "${refs[@]}"
